apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: flux-system
  annotations:
    event.toolkit.fluxcd.io/jira: "KAN-2"
    event.toolkit.fluxcd.io/env: "test"
    event.toolkit.fluxcd.io/env-type: "testing"
    event.toolkit.fluxcd.io/url: "https://github.com/boxcee/forge-flux-deployments"
spec:
  interval: 10m
  chart:
    spec:
      chart: podinfo
      version: "6.6.2"
      sourceRef:
        kind: HelmRepository
        name: podinfo
  values:
    replicaCount: 1
