---
phase: 02-content-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/setup.md
autonomous: true
requirements:
  - ACCY-01
  - ACCY-02
  - ACCY-03

must_haves:
  truths:
    - "FluxCD setup instructions use all 5 annotation keys that src/mapper.js actually reads"
    - "ArgoCD notification template body includes all 7 top-level fields and 4 annotation keys that src/argocd-mapper.js reads"
    - "Annotation reference has separate tables for FluxCD (5 keys) and ArgoCD (4 keys) with correct required/optional flags"
    - "FluxCD section includes HelmRelease annotation example"
    - "ArgoCD section includes trigger configuration and Application annotation example"
    - "WEBHOOK_SECRET and ARGOCD_WEBHOOK_TOKEN env var setup steps are documented"
  artifacts:
    - path: "docs/setup.md"
      provides: "Complete setup guide for FluxCD and ArgoCD"
      contains: "env-type"
  key_links:
    - from: "docs/setup.md FluxCD annotations"
      to: "src/mapper.js extractMetadata"
      via: "documentation accuracy"
      pattern: "env-type|chart-version|event\\.toolkit\\.fluxcd\\.io"
    - from: "docs/setup.md ArgoCD template"
      to: "src/argocd-mapper.js extractMetadata"
      via: "documentation accuracy"
      pattern: "finishedAt|envType|message|healthStatus"
---

<objective>
Rewrite docs/setup.md so FluxCD and ArgoCD instructions match the actual source code behavior.

Purpose: Users following these instructions must produce webhook payloads that the app handles correctly. Every annotation key, template field, and configuration step must match what src/mapper.js and src/argocd-mapper.js actually parse.

Output: Updated docs/setup.md with accurate FluxCD section, accurate ArgoCD section, and complete annotation reference tables.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-accuracy/02-RESEARCH.md

@docs/setup.md
@src/mapper.js
@src/argocd-mapper.js
@src/index.js
@src/hmac.js
@src/bearer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FluxCD setup section and add Forge env var step</name>
  <files>docs/setup.md</files>
  <action>
Rewrite sections 1, 2, and the FluxCD portion of section 4 in docs/setup.md. Keep the existing front matter and page title. Structure:

**Section 1 - Prerequisites:** Keep as-is (already correct).

**Section 2 - FluxCD Configuration:** Rewrite with these subsections:

A. **Set the webhook secret** -- Add a NEW first step: `forge variables set WEBHOOK_SECRET` with the same token used in the Kubernetes secret. Explain that this is the Forge env var the app checks HMAC against. Without this, all webhooks return 401.

B. **Create a Webhook Secret** -- Keep existing kubectl command (already correct).

C. **Configure the Provider** -- Keep existing YAML (already correct: `generic-hmac`, `v1beta3`).

D. **Create the Alert** -- Keep existing YAML (already correct).

E. **Annotate your HelmRelease** -- NEW subsection. Add a complete HelmRelease example showing all 5 annotations with full annotation keys:
```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: my-app
  namespace: production
  annotations:
    event.toolkit.fluxcd.io/jira: "PROJ-123"
    event.toolkit.fluxcd.io/env: "production"
    event.toolkit.fluxcd.io/env-type: "production"
    event.toolkit.fluxcd.io/url: "https://github.com/org/repo"
spec:
  chart:
    spec:
      version: "1.2.3"   # exposed as chart-version in events
```

After the example, add a note explaining: Flux strips the `event.toolkit.fluxcd.io/` prefix when sending webhook payloads, so the app supports both full and short keys. The `helm.toolkit.fluxcd.io/chart-version` annotation is read from the `revision` field in webhook payloads.

F. **Verify it works** -- NEW subsection. Tell users to trigger a HelmRelease reconciliation and check `forge logs` for a 200 response. If 204, check annotations. If 401, check WEBHOOK_SECRET.

**Section 4 - FluxCD Annotation Reference Table:** Replace the single shared table with a FluxCD-specific table listing all 5 annotations:

| Annotation | Short Key | Required | Default | Description |
|---|---|---|---|---|
| `event.toolkit.fluxcd.io/jira` | `jira` | Yes | -- | Comma-separated Jira issue keys. If missing, event is silently skipped (204). |
| `event.toolkit.fluxcd.io/env` | `env` | Yes | -- | Environment name. Returns 400 if missing. |
| `event.toolkit.fluxcd.io/env-type` | `env-type` | No | `unmapped` | Jira environment type. Valid values: `unmapped`, `development`, `testing`, `staging`, `production`. |
| `event.toolkit.fluxcd.io/url` | `url` | No | `''` | URL shown in Jira deployment panel. Recommended -- Jira panel link is broken without it. |
| `helm.toolkit.fluxcd.io/chart-version` | `revision` | No | -- | Chart version used in display name and deployment label. |

Add a subsection "Ignored event reasons" after the table explaining that `UninstallSucceeded` and `DependencyNotReady` events return 204 silently and do not create deployment records. This is intentional.

Do NOT modify the ArgoCD section yet (Task 2 handles that). Leave sections 3 and the ArgoCD part of section 4 as-is for now.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>FluxCD section documents all 5 annotation keys with correct required/optional flags, includes HelmRelease annotation example, documents WEBHOOK_SECRET setup, mentions ignored reasons, and has a FluxCD-specific annotation reference table.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ArgoCD setup section and add ArgoCD annotation reference table</name>
  <files>docs/setup.md</files>
  <action>
Rewrite section 3 (ArgoCD) and add the ArgoCD annotation reference table in section 4. Structure:

**Section 3 - ArgoCD Configuration:** Rewrite with these subsections:

A. **Set the webhook token** -- NEW first step: `forge variables set ARGOCD_WEBHOOK_TOKEN` with the bearer token. Explain this is checked against the `Authorization: Bearer <token>` header. Without it, all webhooks return 401.

B. **Configure the webhook service** -- Keep existing ConfigMap YAML structure but ensure headers show `Authorization: Bearer <your-token>`.

C. **Define the notification template** -- REPLACE the template body with the corrected version that includes ALL fields the code reads:
```yaml
template.jira-deployment: |
  webhook:
    jira-deployments:
      method: POST
      body: |
        {
          "app": "{{.app.metadata.name}}",
          "namespace": "{{.app.spec.destination.namespace}}",
          "revision": "{{.app.status.sync.revision}}",
          "phase": "{{.app.status.operationState.phase}}",
          "healthStatus": "{{.app.status.health.status}}",
          "finishedAt": "{{.app.status.operationState.finishedAt}}",
          "message": "{{.app.status.operationState.message}}",
          "annotations": {
            "jira": "{{index .app.metadata.annotations \"jira\"}}",
            "env": "{{index .app.metadata.annotations \"env\"}}",
            "envType": "{{index .app.metadata.annotations \"envType\"}}",
            "url": "{{index .app.metadata.annotations \"url\"}}"
          }
        }
```

D. **Configure the trigger** -- NEW subsection. Add the trigger condition:
```yaml
trigger.on-sync-status: |
  - when: app.status.operationState.phase in ['Succeeded', 'Failed', 'Error', 'Running']
    send: [jira-deployment]
```

E. **Annotate your Application** -- NEW subsection. Add complete Application example:
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    notifications.argoproj.io/subscribe.on-sync-status.jira-deployments: ""
    jira: "PROJ-123"
    env: "production"
    envType: "production"
    url: "https://argocd.example.com/applications/my-app"
```

Note: ArgoCD uses flat annotation keys (no prefix stripping). `envType` is camelCase (not hyphenated like FluxCD's `env-type`).

F. **Verify it works** -- NEW subsection. Tell users to trigger a sync and check `forge logs`.

**Section 4 - ArgoCD Annotation Reference Table:** Add a separate ArgoCD table:

| Annotation | Required | Default | Description |
|---|---|---|---|
| `jira` | Yes | -- | Comma-separated Jira issue keys. If missing, event is silently skipped (204). |
| `env` | Yes | -- | Environment name. Returns 400 if missing. |
| `envType` | No | `unmapped` | Jira environment type. Valid values: `unmapped`, `development`, `testing`, `staging`, `production`. Note: camelCase, not hyphenated. |
| `url` | No | `''` | URL shown in Jira deployment panel. Recommended. |

Also add a subsection "ArgoCD payload fields" listing the top-level fields (`app`, `namespace`, `revision`, `phase`, `healthStatus`, `finishedAt`, `message`) and what Jira state each `phase` value maps to (Succeeded->successful, Failed/Error->failed, Running->in_progress, other->unknown).

Rename section 4 heading to "Annotation Reference" (no longer "Annotate Your Resources") and restructure it as two clearly labeled subsections: "FluxCD Annotations" and "ArgoCD Annotations".
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>ArgoCD section includes corrected template body with all 7 top-level fields and 4 annotation keys, trigger configuration, Application annotation example, ARGOCD_WEBHOOK_TOKEN setup, and a separate ArgoCD annotation reference table with envType (camelCase) documented.</done>
</task>

</tasks>

<verification>
Cross-check docs/setup.md against source code:
1. Count FluxCD annotation keys in docs -- must be 5 (jira, env, env-type, url, chart-version)
2. Count ArgoCD annotation keys in docs -- must be 4 (jira, env, envType, url)
3. Count ArgoCD template body fields -- must be 7 top-level + 4 annotations
4. Verify `WEBHOOK_SECRET` and `ARGOCD_WEBHOOK_TOKEN` env var setup documented
5. `npm test` passes (no source code modified)
</verification>

<success_criteria>
- docs/setup.md FluxCD section uses all 5 annotation keys from src/mapper.js
- docs/setup.md ArgoCD template body includes all fields from src/argocd-mapper.js
- Separate annotation reference tables for FluxCD and ArgoCD
- Required/optional flags match code behavior
- WEBHOOK_SECRET and ARGOCD_WEBHOOK_TOKEN setup steps present
- npm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-accuracy/02-01-SUMMARY.md`
</output>
