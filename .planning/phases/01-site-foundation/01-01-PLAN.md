---
phase: 01-site-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_config.yml
  - docs/index.md
  - docs/setup.md
  - docs/privacy-policy.md
  - docs/terms-of-service.md
  - docs/troubleshooting.md
autonomous: true
requirements:
  - SITE-01
  - SITE-02

must_haves:
  truths:
    - "Site uses just-the-docs theme with sidebar navigation and search"
    - "Sidebar shows 5 pages in order: Home, Setup, Troubleshooting, Privacy, Terms"
    - "Index page is a feature showcase with value proposition, how-it-works steps, and deployment states table"
    - "marketplace-listing.md and plans/ are excluded from the published site"
    - "Terms of service references ELv2 instead of MIT"
  artifacts:
    - path: "docs/_config.yml"
      provides: "just-the-docs theme config with search, aux_links, footer, exclude list"
      contains: "remote_theme: just-the-docs/just-the-docs"
    - path: "docs/index.md"
      provides: "Feature showcase home page"
      contains: "nav_order: 1"
    - path: "docs/setup.md"
      provides: "Setup page with nav front matter"
      contains: "nav_order: 2"
    - path: "docs/troubleshooting.md"
      provides: "Troubleshooting placeholder page"
      contains: "nav_order: 3"
    - path: "docs/privacy-policy.md"
      provides: "Privacy page with nav front matter"
      contains: "nav_order: 4"
    - path: "docs/terms-of-service.md"
      provides: "Terms page with nav front matter and ELv2 reference"
      contains: "nav_order: 5"
  key_links:
    - from: "docs/_config.yml"
      to: "just-the-docs/just-the-docs"
      via: "remote_theme directive"
      pattern: "remote_theme: just-the-docs/just-the-docs"
    - from: "docs/_config.yml"
      to: "docs/marketplace-listing.md"
      via: "exclude list"
      pattern: "exclude:.*marketplace-listing\\.md"
---

<objective>
Migrate the documentation site from jekyll-theme-minimal to just-the-docs with sidebar navigation, search, and a feature showcase home page.

Purpose: SITE-01 and SITE-02 -- the docs site must render on GitHub Pages with just-the-docs theme, working sidebar nav, search, and no broken links.
Output: Fully configured _config.yml, front matter on all pages, rewritten index.md, troubleshooting placeholder, ToS license ref updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation/01-CONTEXT.md
@.planning/phases/01-site-foundation/01-RESEARCH.md

Source files to modify:
@docs/_config.yml
@docs/index.md
@docs/setup.md
@docs/privacy-policy.md
@docs/terms-of-service.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite _config.yml and add front matter to all existing pages</name>
  <files>docs/_config.yml, docs/setup.md, docs/privacy-policy.md, docs/terms-of-service.md, docs/troubleshooting.md</files>
  <action>
1. **Rewrite `docs/_config.yml`** -- replace entire contents with:
   - `title: GitOps Deployments for Jira`
   - `description: Track FluxCD and ArgoCD deployments directly in Jira issues.`
   - `remote_theme: just-the-docs/just-the-docs`
   - `plugins: [jekyll-remote-theme]`
   - `color_scheme: default`
   - `logo: /assets/icon.svg`
   - `search_enabled: true` with `search.heading_level: 2`, `search.previews: 3`
   - `heading_anchors: true`
   - `aux_links:` with `"Install from Marketplace": ["https://marketplace.atlassian.com/"]`
   - `aux_links_new_tab: true`
   - `footer_content: "&copy; 2026 GitOps Deployments for Jira"`
   - `exclude: [marketplace-listing.md, plans/]`
   - Remove ALL old keys: `theme`, `show_downloads`, `google_analytics`, old `remote_theme`

2. **Add front matter to `docs/setup.md`** -- prepend YAML front matter block:
   ```yaml
   ---
   title: Setup
   nav_order: 2
   description: "Connect FluxCD or ArgoCD to Jira's native Deployments panel."
   ---
   ```
   Keep existing content below. Remove the manual "Back to Home" link at bottom (sidebar handles navigation).

3. **Add front matter to `docs/privacy-policy.md`** -- prepend:
   ```yaml
   ---
   title: Privacy
   nav_order: 4
   description: "How the app handles your data."
   ---
   ```
   Keep existing content.

4. **Update `docs/terms-of-service.md`** -- prepend front matter AND fix license reference:
   ```yaml
   ---
   title: Terms
   nav_order: 5
   description: "End user terms of service."
   ---
   ```
   In Section 1 (License), replace "MIT License (or the license specified in the source repository)" with "Elastic License 2.0 (ELv2)". The sentence should read: "The App is provided as-is under the Elastic License 2.0 (ELv2). You are granted a non-exclusive right to use the App within your Jira Cloud instance."

5. **Create `docs/troubleshooting.md`** -- minimal placeholder for Phase 1 (full content is Phase 2 SITE-03):
   ```yaml
   ---
   title: Troubleshooting
   nav_order: 3
   description: "Common issues and solutions."
   ---
   ```
   Body: heading "# Troubleshooting", a brief intro sentence ("Common issues and solutions for GitOps Deployments for Jira."), then "Detailed troubleshooting content coming soon." This ensures the sidebar entry exists and links work.
  </action>
  <verify>
    <automated>grep -q "remote_theme: just-the-docs/just-the-docs" docs/_config.yml && grep -q "nav_order: 2" docs/setup.md && grep -q "nav_order: 3" docs/troubleshooting.md && grep -q "nav_order: 4" docs/privacy-policy.md && grep -q "nav_order: 5" docs/terms-of-service.md && grep -q "Elastic License 2.0" docs/terms-of-service.md && grep -q "marketplace-listing.md" docs/_config.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>_config.yml uses just-the-docs via remote_theme with search, aux_links, footer, and exclude list. All 4 existing pages have correct nav_order front matter. Troubleshooting placeholder exists at nav_order 3. ToS references ELv2 instead of MIT. No old minimal-theme keys remain.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite index.md as feature showcase home page</name>
  <files>docs/index.md</files>
  <action>
Replace `docs/index.md` entirely with a feature showcase page per locked decisions:

1. **Front matter:**
   ```yaml
   ---
   title: Home
   nav_order: 1
   description: "Track FluxCD and ArgoCD deployments directly in Jira issues."
   ---
   ```

2. **Hero/value proposition:** Lead heading "GitOps Deployments for Jira". Subheading or lead paragraph: "Track FluxCD and ArgoCD deployments directly in Jira issues. See deployment status, environment, and version in the native Deployments panel -- no polling, no extra dashboards."

3. **Supported CD tools:** Brief mention that both FluxCD (HMAC webhooks) and ArgoCD (bearer token webhooks) are supported.

4. **How it works:** Numbered list, 4 steps:
   1. Install the app from the Atlassian Marketplace
   2. Configure your CD tool to send webhook events
   3. Annotate your resources with Jira issue keys
   4. See deployments appear in Jira's native panel

5. **Deployment states table:** Show the states the app reports to Jira:

   | State | Description |
   |-------|-------------|
   | `successful` | Deployment completed successfully |
   | `failed` | Deployment failed |
   | `in_progress` | Deployment is in progress |
   | `rolled_back` | Deployment was rolled back |

6. **Links section:** "Get started" link to `./setup`, support link to GitHub issues.

Do NOT include: table of contents (just-the-docs handles this), manual footer (config handles it), "Back to Home" links.
  </action>
  <verify>
    <automated>grep -q "nav_order: 1" docs/index.md && grep -q "How it works" docs/index.md && grep -q "successful" docs/index.md && grep -q "rolled_back" docs/index.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Index page is a feature showcase with value proposition, how-it-works numbered list, deployment states table, and links to setup guide. Front matter has nav_order 1 and title "Home".</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 6 docs/*.md files have correct `nav_order` front matter (1 through 5, plus marketplace-listing excluded)
2. `_config.yml` has no remnants of jekyll-theme-minimal
3. `grep -c "nav_order:" docs/*.md` returns 5 (index, setup, troubleshooting, privacy, terms -- not marketplace-listing)
4. `grep "show_downloads\|google_analytics\|jekyll-theme-minimal" docs/_config.yml` returns nothing
</verification>

<success_criteria>
- docs/_config.yml configures just-the-docs with search, aux_links, footer, and file exclusions
- All 5 sidebar pages have nav_order front matter in correct order (1-5)
- Index page is a feature showcase (not a link list)
- ToS Section 1 references Elastic License 2.0
- Troubleshooting placeholder exists for sidebar completeness
- No old minimal-theme config keys remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-foundation/01-01-SUMMARY.md`
</output>
