---
phase: 10-fix-argocd-source-mismatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.js
  - src/__tests__/index.test.js
  - src/__tests__/resolver.test.js
autonomous: true
requirements: [UI-02, UI-03]
gap_closure: true

must_haves:
  truths:
    - "ArgoCD events are stored with source value 'argocd' in Forge SQL"
    - "Selecting Argo filter in admin UI returns ArgoCD events in both stats strip and event table"
    - "Source value stored by handler matches the value sent by frontend filter"
  artifacts:
    - path: "src/index.js"
      provides: "ArgoCD handler with source: 'argocd'"
      contains: "source: 'argocd'"
    - path: "src/__tests__/index.test.js"
      provides: "Updated test assertions for source: 'argocd'"
      contains: "source: 'argocd'"
    - path: "src/__tests__/resolver.test.js"
      provides: "Updated resolver test using source: 'argocd'"
      contains: "source: 'argocd'"
  key_links:
    - from: "src/index.js handleArgoEvent"
      to: "src/frontend/index.jsx source filter"
      via: "Matching source value 'argocd' in SQL WHERE clause"
      pattern: "source: 'argocd'"
---

<objective>
Fix ArgoCD source value mismatch between backend handler and frontend filter.

Purpose: The backend stores ArgoCD events with `source: 'argo'` but the frontend filter sends `source: 'argocd'`. This causes the Argo filter in the admin UI to return zero results for both the stats strip and event table.

Output: Backend handler stores `'argocd'`, matching what the frontend already sends. All tests updated. Argo filter works end-to-end.
</objective>

<execution_context>
@/Users/moritzschmitz-oviva/workspaces/forge-flux-deployments/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moritzschmitz-oviva/workspaces/forge-flux-deployments/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/08-admin-ui-event-log-tab/08-01-SUMMARY.md

<interfaces>
<!-- Frontend source filter values (these are correct and must NOT change) -->

From src/frontend/index.jsx:
```jsx
// Line 32: formatSource maps stored value to display label
if (src === 'argocd') return 'Argo';

// Line 124-128: Select filter options
{ label: 'All sources', value: '' },
{ label: 'Flux', value: 'flux' },
{ label: 'Argo', value: 'argocd' },
```

From src/index.js:
```js
// Line 35: Flux handler (correct — 'flux' matches frontend)
const logParams = { source: 'flux' };

// Line 121: ArgoCD handler (BUG — 'argo' does NOT match frontend 'argocd')
const logParams = { source: 'argo' };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ArgoCD source value in handler and update all test assertions</name>
  <files>src/index.js, src/__tests__/index.test.js, src/__tests__/resolver.test.js</files>
  <read_first>
    src/index.js
    src/__tests__/index.test.js
    src/__tests__/resolver.test.js
    src/frontend/index.jsx
  </read_first>
  <behavior>
    - Test: handleArgoEvent stores source 'argocd' on successful webhook (line 259 of index.test.js)
    - Test: handleArgoEvent stores source 'argocd' on Jira API failure (line 272 of index.test.js)
    - Test: resolver getEventStats passes source 'argocd' through (lines 245-246 of resolver.test.js)
    - All existing tests continue to pass with updated assertions
  </behavior>
  <action>
    1. In `src/index.js`, change line 121 from `const logParams = { source: 'argo' };` to `const logParams = { source: 'argocd' };`. This is the ONLY production code change needed.

    2. In `src/__tests__/index.test.js`, update two test assertions:
       - Line 259: change `source: 'argo'` to `source: 'argocd'` in the `expect.objectContaining` call
       - Line 272: change `source: 'argo'` to `source: 'argocd'` in the `expect.objectContaining` call

    3. In `src/__tests__/resolver.test.js`, update the getEventStats test:
       - Line 245: change `{ source: 'argo' }` to `{ source: 'argocd' }` in the invoke call
       - Line 246: change `{ source: 'argo' }` to `{ source: 'argocd' }` in the expect call

    Do NOT change:
    - `src/frontend/index.jsx` — already correct with `value: 'argocd'`
    - `src/event-log.js` — uses parameterized queries, no hardcoded source values
    - Any Flux-related source values — `'flux'` is correct
  </action>
  <verify>
    <automated>cd /Users/moritzschmitz-oviva/workspaces/forge-flux-deployments && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/index.js contains `source: 'argocd'` at the handleArgoEvent logParams initialization (no remaining `source: 'argo'` without the 'cd' suffix in ArgoCD handler)
    - src/__tests__/index.test.js contains `source: 'argocd'` in both ArgoCD test assertions (lines ~259 and ~272)
    - src/__tests__/resolver.test.js contains `source: 'argocd'` in the getEventStats test (lines ~245-246)
    - `npm test` exits 0 with all tests passing
    - `npm run lint` exits 0
    - grep -c "source: 'argo'" src/index.js returns 0 (no bare 'argo' without 'cd' suffix)
    - grep -c "source: 'argocd'" src/index.js returns 1
  </acceptance_criteria>
  <done>ArgoCD handler stores source value 'argocd' matching frontend filter. All tests pass with updated assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end source value alignment</name>
  <files>src/index.js, src/frontend/index.jsx</files>
  <read_first>
    src/index.js
    src/frontend/index.jsx
  </read_first>
  <action>
    Run a final cross-file verification to confirm source values are aligned:

    1. Confirm `src/index.js` handleFluxEvent uses `source: 'flux'` and handleArgoEvent uses `source: 'argocd'`
    2. Confirm `src/frontend/index.jsx` filter options use `value: 'flux'` and `value: 'argocd'`
    3. Confirm `src/frontend/index.jsx` formatSource maps `'flux'` to `'Flux'` and `'argocd'` to `'Argo'`
    4. Run `npm test` and `npm run lint` one final time to confirm clean state

    No file modifications expected in this task — verification only.
  </action>
  <verify>
    <automated>cd /Users/moritzschmitz-oviva/workspaces/forge-flux-deployments && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep "source: 'flux'" src/index.js returns exactly 1 match (handleFluxEvent)
    - grep "source: 'argocd'" src/index.js returns exactly 1 match (handleArgoEvent)
    - grep "value: 'flux'" src/frontend/index.jsx returns exactly 1 match
    - grep "value: 'argocd'" src/frontend/index.jsx returns exactly 1 match
    - npm test exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <done>Source values are aligned across backend and frontend. Argo filter will match stored events in SQL queries.</done>
</task>

</tasks>

<verification>
1. `npm test` passes — all test suites green
2. `npm run lint` passes — no lint errors
3. `grep "source: 'argo'" src/index.js` returns NO matches (only `'argocd'` for Argo handler)
4. Source values match: backend `'argocd'` == frontend filter `'argocd'`
</verification>

<success_criteria>
- ArgoCD events stored with source='argocd' matching frontend filter value
- All tests pass with updated assertions
- Lint clean
- UI-02 and UI-03 requirements fully satisfied (Argo filter returns matching rows)
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-argocd-source-mismatch/10-01-SUMMARY.md`
</output>
