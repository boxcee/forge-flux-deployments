---
phase: 04-marketplace-listing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/marketplace-listing.md
autonomous: true
requirements:
  - MRKT-01
  - MRKT-02
  - MRKT-03

must_haves:
  truths:
    - "marketplace-listing.md contains zero TODO placeholders"
    - "All URLs in marketplace-listing.md return HTTP 200"
    - "Every listing claim about deployment states matches source code behavior"
    - "Summary field is 170 characters or fewer"
    - "App icon is 144x144 PNG RGBA (docs/assets/icon.png confirmed as Marketplace icon)"
  artifacts:
    - path: "docs/marketplace-listing.md"
      provides: "Complete, accurate Marketplace listing ready for submission"
      contains: "successful, failed, rolled back"
  key_links:
    - from: "docs/marketplace-listing.md"
      to: "src/mapper.js"
      via: "deployment state claims must match REASON_TO_STATE mapping"
      pattern: "successful.*failed.*rolled.back"
    - from: "docs/marketplace-listing.md"
      to: "src/argocd-mapper.js"
      via: "ArgoCD-specific claims (in_progress) must be scoped to ArgoCD only"
      pattern: "in.progress"
---

<objective>
Finalize marketplace-listing.md with accurate content and verified URLs, and confirm app icon meets Atlassian Marketplace specifications.

Purpose: Make listing materials submission-ready so Atlassian approves on first attempt.
Output: Updated docs/marketplace-listing.md with accurate deployment state descriptions, verified URLs, and confirmed icon selection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-marketplace-listing/04-RESEARCH.md

@docs/marketplace-listing.md
@src/mapper.js
@src/argocd-mapper.js
@manifest.yml

<interfaces>
<!-- Source code deployment state mappings for cross-checking listing claims -->

From src/mapper.js (FluxCD):
- States: successful, failed, rolled_back, unknown
- NO in_progress state for FluxCD
- Ignored reasons: UninstallSucceeded, DependencyNotReady

From src/argocd-mapper.js (ArgoCD):
- States: successful, failed, in_progress, unknown
- Running -> in_progress (only ArgoCD has this)

From docs/assets/icon.png:
- 144x144 PNG RGBA (blue chiclet background)
- Meets Atlassian Marketplace specs (144x144 PNG, transparent/chiclet bg)
- No trademarked imagery
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-check and fix listing content accuracy</name>
  <files>docs/marketplace-listing.md</files>
  <action>
Cross-check every claim in marketplace-listing.md against source code and fix inaccuracies:

1. **Fix "in progress" deployment state claim** (known inaccuracy from research):
   - Current: "Deployment status (successful, failed, in progress, rolled back) shown on Jira issues"
   - Problem: FluxCD does NOT have `in_progress`. Only ArgoCD maps `Running` to `in_progress`.
   - Fix: Change to accurately reflect per-tool states, e.g.: "Deployment status shown on Jira issues — successful, failed, and rolled back for FluxCD; successful, failed, in progress, and rolled back for ArgoCD"
   - OR simplify to the common set and note the ArgoCD extra: "Deployment status (successful, failed, rolled back) on Jira issues. ArgoCD additionally tracks in-progress deployments."

2. **Verify all other claims** against the cross-check table from 04-RESEARCH.md:
   - "FluxCD — HelmRelease events via generic-hmac webhooks" → confirm against src/index.js, src/hmac.js
   - "ArgoCD — Application sync events via ArgoCD Notifications webhooks" → confirm against src/index.js, src/bearer.js
   - "Environment tracking" → confirm both mappers extract `env` annotation
   - "Revision tracking (Helm chart versions for Flux, Git SHAs for Argo)" → confirm src/mapper.js uses `chartVersion`, src/argocd-mapper.js uses `revision`
   - "No agents, no database, no external infrastructure" → confirm manifest.yml has no remote hostnames
   - "HMAC-signed webhooks for Flux, bearer token auth for Argo" → confirm src/hmac.js and src/bearer.js exist

3. **Verify URLs resolve** by running curl against each URL in the file:
   - https://github.com/boxcee/forge-flux-deployments/issues
   - https://boxcee.github.io/forge-flux-deployments/privacy-policy
   - https://boxcee.github.io/forge-flux-deployments/terms-of-service

4. **Verify summary length**: Count characters in Summary field. Must be ≤170. Current is exactly 170 — if any edits spill into summary, re-count.

5. **Add icon reference** at the bottom of the file (after Security Program section):
   ```
   ## App Icon
   File: `docs/assets/icon.png`
   Dimensions: 144 x 144 px
   Format: PNG (RGBA)
   Background: Blue chiclet (rounded rectangle)
   ```
   This makes the icon selection explicit for whoever pastes into Marketplace portal.
  </action>
  <verify>
    <automated>cd /Users/moritzschmitz-oviva/workspaces/forge-flux-deployments && grep -c "TODO\|todo\|PLACEHOLDER\|placeholder" docs/marketplace-listing.md | grep -q "^0$" && echo "PASS: No TODOs" && curl -s -o /dev/null -w "%{http_code}" https://boxcee.github.io/forge-flux-deployments/privacy-policy | grep -q "200" && echo "PASS: Privacy policy URL" && curl -s -o /dev/null -w "%{http_code}" https://boxcee.github.io/forge-flux-deployments/terms-of-service | grep -q "200" && echo "PASS: Terms URL" && curl -s -o /dev/null -w "%{http_code}" https://github.com/boxcee/forge-flux-deployments/issues | grep -q "200" && echo "PASS: Support URL"</automated>
  </verify>
  <done>All URLs return HTTP 200. Deployment state description accurately reflects per-tool states (no false "in progress" claim for FluxCD). Zero TODO placeholders. Summary ≤170 chars. Icon section added.</done>
</task>

<task type="auto">
  <name>Task 2: Validate app icon meets Marketplace specifications</name>
  <files>docs/marketplace-listing.md</files>
  <action>
Validate that docs/assets/icon.png meets all Atlassian Marketplace icon requirements:

1. **Dimensions**: Run `sips -g pixelWidth -g pixelHeight docs/assets/icon.png` — must be exactly 144x144.
2. **Format**: Run `file docs/assets/icon.png` — must show "PNG image data" with "RGBA" (transparency support).
3. **Content check**: The icon uses a custom deployment arrow design on blue chiclet background. Confirm no Atlassian logos, no FluxCD/ArgoCD logos (per Atlassian brand guidelines).
4. **Background style**: RGBA with blue rounded-rectangle background = chiclet style, which Atlassian explicitly allows.

If all checks pass, no changes needed — the icon is ready. If any check fails, flag it as a blocker.

Note: The `logo-144.png` and `logo-144-transparent.png` files at repo root are NOT used for Marketplace submission. They have dark RGB backgrounds (not RGBA/transparent). The docs/assets/icon.png is the confirmed choice per research recommendation.
  </action>
  <verify>
    <automated>cd /Users/moritzschmitz-oviva/workspaces/forge-flux-deployments && sips -g pixelWidth -g pixelHeight docs/assets/icon.png 2>&1 | grep -q "pixelWidth: 144" && sips -g pixelWidth -g pixelHeight docs/assets/icon.png 2>&1 | grep -q "pixelHeight: 144" && file docs/assets/icon.png | grep -q "RGBA" && echo "PASS: Icon meets Marketplace specs (144x144 RGBA PNG)"</automated>
  </verify>
  <done>docs/assets/icon.png confirmed as 144x144 RGBA PNG with chiclet background. No trademarked imagery. Ready for Marketplace submission.</done>
</task>

</tasks>

<verification>
1. `grep -c "TODO\|todo\|PLACEHOLDER" docs/marketplace-listing.md` returns 0
2. All 3 URLs in marketplace-listing.md return HTTP 200
3. Deployment state description distinguishes FluxCD states from ArgoCD states (no false "in progress" for Flux)
4. Summary field ≤170 characters
5. docs/assets/icon.png is 144x144 RGBA PNG
6. Icon section present in marketplace-listing.md
</verification>

<success_criteria>
marketplace-listing.md is complete and accurate — every claim matches source code, every URL resolves, the icon selection is documented, and the file is ready to paste into the Atlassian Marketplace portal.
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-listing/04-01-SUMMARY.md`
</output>
