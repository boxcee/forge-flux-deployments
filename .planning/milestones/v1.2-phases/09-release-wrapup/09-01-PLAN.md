---
phase: 09-release-wrapup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - package.json
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release.yml
  - .github/workflows/deploy.yml
  - docs/setup.md
autonomous: true
requirements: [MAINT-03, HK-01, HK-02]
user_setup:
  - service: github
    why: "CI/CD deployment pipeline requires Forge CLI auth secrets"
    env_vars:
      - name: FORGE_EMAIL
        source: "GitHub repo Settings -> Secrets and variables -> Actions -> New repository secret"
      - name: FORGE_API_TOKEN
        source: "GitHub repo Settings -> Secrets and variables -> Actions -> New repository secret. Value from https://id.atlassian.com/manage-profile/security/api-tokens"

must_haves:
  truths:
    - "CHANGELOG.md exists with v1.0.0, v1.1.0, and v1.2.0 entries"
    - "package.json reports version 1.2.0"
    - "release-please is configured to manage future releases from v1.2.0"
    - "Push to main triggers development deploy via GitHub Actions"
    - "GitHub Release triggers production deploy via GitHub Actions"
    - "docs/setup.md documents the Event Log tab feature"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Retroactive changelog entries for all milestones"
      contains: "## [1.2.0]"
    - path: "package.json"
      provides: "Version 1.2.0"
      contains: "\"version\": \"1.2.0\""
    - path: "release-please-config.json"
      provides: "Release-please manifest mode config"
      contains: "release-type"
    - path: ".release-please-manifest.json"
      provides: "Release-please version tracking"
      contains: "\".\": \"1.2.0\""
    - path: ".github/workflows/release.yml"
      provides: "Release-please GitHub Action"
      contains: "googleapis/release-please-action@v4"
    - path: ".github/workflows/deploy.yml"
      provides: "Forge deploy pipeline"
      contains: "forge deploy"
    - path: "docs/setup.md"
      provides: "Event Log documentation"
      contains: "Event Log"
  key_links:
    - from: ".release-please-manifest.json"
      to: "package.json"
      via: "version alignment"
      pattern: "1\\.2\\.0"
    - from: ".github/workflows/deploy.yml"
      to: "FORGE_EMAIL secret"
      via: "env vars in workflow"
      pattern: "secrets\\.FORGE_EMAIL"
---

<objective>
Create CHANGELOG.md with retroactive entries, bump version to 1.2.0, set up release-please for future automation, add GitHub Actions CI/CD workflows, and update documentation for the event log feature.

Purpose: Close out v1.2 milestone with proper versioning, release automation, and documentation so future releases are zero-touch.
Output: CHANGELOG.md, updated package.json, release-please config files, two GitHub Actions workflows, updated docs/setup.md
</objective>

<execution_context>
@/Users/moritzschmitz-oviva/workspaces/forge-flux-deployments/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moritzschmitz-oviva/workspaces/forge-flux-deployments/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-wrapup/09-CONTEXT.md
@.planning/phases/09-release-wrapup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG, bump version, configure release-please</name>
  <files>CHANGELOG.md, package.json, release-please-config.json, .release-please-manifest.json</files>
  <read_first>package.json, .planning/phases/09-release-wrapup/09-RESEARCH.md</read_first>
  <action>
1. Create `CHANGELOG.md` with retroactive entries in Keep a Changelog format. Use the exact content below:

```markdown
# Changelog

## [1.2.0](https://github.com/boxcee/forge-flux-deployments/compare/v1.1.0...v1.2.0) (2026-03-16)

### Features

* Webhook event logging to Forge SQL with 30-day retention
* Admin page Event Log tab with stats, filtering, and keyset pagination
* Daily scheduled cleanup of old events

## [1.1.0](https://github.com/boxcee/forge-flux-deployments/compare/v1.0.0...v1.1.0) (2026-03-12)

### Features

* Admin configuration page for webhook secrets
* KVS-based secret storage with admin UI management
* ArgoCD webhook support with bearer token auth

## [1.0.0](https://github.com/boxcee/forge-flux-deployments/releases/tag/v1.0.0) (2026-03-11)

### Features

* FluxCD webhook receiver with HMAC verification
* Jira Deployments API integration
* HelmRelease annotation-based metadata extraction
* Marketplace listing and documentation site
```

2. Update `package.json` — change `"version": "0.1.0"` to `"version": "1.2.0"`. Do NOT change any other field.

3. Create `release-please-config.json`:
```json
{
  "packages": {
    ".": {
      "release-type": "node",
      "changelog-path": "CHANGELOG.md"
    }
  }
}
```

4. Create `.release-please-manifest.json`:
```json
{
  ".": "1.2.0"
}
```

5. Create git tags for release-please bootstrap. Run:
   - `git tag v1.0.0` (on current HEAD — retroactive, no existing v1.0 tag)
   - The existing `v1.1` tag stays as-is. Create `v1.1.0` on the same commit: `git tag v1.1.0 v1.1`
   - Do NOT create v1.2.0 tag yet — that will be created when release-please merges its first Release PR after this phase is committed.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));process.exit(p.version==='1.2.0'?0:1)" && grep -q '## \[1.2.0\]' CHANGELOG.md && grep -q '## \[1.1.0\]' CHANGELOG.md && grep -q '## \[1.0.0\]' CHANGELOG.md && grep -q 'release-type' release-please-config.json && grep -q '"1.2.0"' .release-please-manifest.json && git tag -l | grep -q 'v1.0.0' && git tag -l | grep -q 'v1.1.0'</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"version": "1.2.0"`
    - CHANGELOG.md contains `## [1.2.0]`, `## [1.1.0]`, and `## [1.0.0]` section headers
    - CHANGELOG.md v1.2.0 entry mentions "event logging", "Event Log tab", "scheduled cleanup"
    - CHANGELOG.md v1.1.0 entry mentions "Admin configuration page", "ArgoCD"
    - CHANGELOG.md v1.0.0 entry mentions "FluxCD webhook receiver", "Jira Deployments API"
    - release-please-config.json contains `"release-type": "node"`
    - .release-please-manifest.json contains `".": "1.2.0"`
    - `git tag -l` output includes `v1.0.0` and `v1.1.0`
    - `npm test` passes (no regressions)
  </acceptance_criteria>
  <done>CHANGELOG.md has 3 versioned sections, package.json is 1.2.0, release-please config bootstrapped at 1.2.0, retroactive git tags created</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI/CD workflows</name>
  <files>.github/workflows/release.yml, .github/workflows/deploy.yml</files>
  <read_first>.planning/phases/09-release-wrapup/09-RESEARCH.md</read_first>
  <action>
1. Create directory `.github/workflows/` if it does not exist.

2. Create `.github/workflows/release.yml` with exact content:

```yaml
name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
```

3. Create `.github/workflows/deploy.yml` with exact content:

```yaml
name: Deploy

on:
  push:
    branches: [main]
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - run: npm install -g @forge/cli@12

      - name: Deploy to development
        if: github.event_name == 'push'
        run: forge deploy --environment development --non-interactive
        env:
          FORGE_EMAIL: ${{ secrets.FORGE_EMAIL }}
          FORGE_API_TOKEN: ${{ secrets.FORGE_API_TOKEN }}

      - name: Deploy to production
        if: github.event_name == 'release'
        run: forge deploy --environment production --non-interactive
        env:
          FORGE_EMAIL: ${{ secrets.FORGE_EMAIL }}
          FORGE_API_TOKEN: ${{ secrets.FORGE_API_TOKEN }}
```

Key details:
- Node version `22` matches the project runtime (nodejs22.x in manifest.yml)
- `@forge/cli@12` pinned to major version to avoid CI breakage
- `--non-interactive` flag prevents hanging on prompts in CI
- `permissions` block on release.yml grants `contents: write` and `pull-requests: write` for release-please to create PRs and releases
- Deploy workflow uses conditional steps: `push` event deploys to development, `release` event deploys to production
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml && test -f .github/workflows/deploy.yml && grep -q 'googleapis/release-please-action@v4' .github/workflows/release.yml && grep -q 'forge deploy --environment development --non-interactive' .github/workflows/deploy.yml && grep -q 'forge deploy --environment production --non-interactive' .github/workflows/deploy.yml && grep -q 'FORGE_EMAIL' .github/workflows/deploy.yml && grep -q 'FORGE_API_TOKEN' .github/workflows/deploy.yml && grep -q 'node-version.*22' .github/workflows/deploy.yml && grep -q '@forge/cli@12' .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` exists and contains `googleapis/release-please-action@v4`
    - `.github/workflows/release.yml` contains `permissions:` block with `contents: write` and `pull-requests: write`
    - `.github/workflows/release.yml` triggers on `push` to `main`
    - `.github/workflows/deploy.yml` exists and contains two `forge deploy` steps
    - `.github/workflows/deploy.yml` contains `forge deploy --environment development --non-interactive` with `if: github.event_name == 'push'`
    - `.github/workflows/deploy.yml` contains `forge deploy --environment production --non-interactive` with `if: github.event_name == 'release'`
    - `.github/workflows/deploy.yml` references `secrets.FORGE_EMAIL` and `secrets.FORGE_API_TOKEN`
    - `.github/workflows/deploy.yml` uses `node-version: '22'` and `@forge/cli@12`
  </acceptance_criteria>
  <done>Two GitHub Actions workflows created: release-please for automated versioning, deploy for Forge CI/CD</done>
</task>

<task type="auto">
  <name>Task 3: Add Event Log documentation to setup guide</name>
  <files>docs/setup.md</files>
  <read_first>docs/setup.md</read_first>
  <action>
Add a new section `## 5. Event Log` to `docs/setup.md` after the existing `## 4. Annotation Reference` section (append at the end of the file). Use this exact content:

```markdown

---

## 5. Event Log

The app records every webhook invocation in a local event log. Admins can view this log directly in the Jira admin page.

### Viewing the Event Log

1. Go to **Jira Settings** (gear icon) > **Apps** > **GitOps Deployments** in the left sidebar.
2. Click the **Event Log** tab.

### What you'll see

- **Stats strip** -- Accepted, failed, and skipped event counts for the last 24 hours.
- **Source filter** -- Filter events by source (All, Flux, Argo).
- **Event table** -- Each row shows timestamp, source, release name, namespace, environment, status code, and deployment state.
- **Pagination** -- The table loads 25 rows at a time. Click **Load more** to fetch additional rows.

### Status codes

| Code | Meaning | Badge color |
|------|---------|-------------|
| 200 | Deployment record created | Green |
| 204 | Event skipped (missing annotations or ignored reason) | Grey |
| 4xx | Client error (auth failure, bad request) | Red |
| 5xx | Server error | Red |

### Retention

Events are automatically deleted after 30 days. A scheduled job runs daily to prune old records.
```

Do NOT modify any existing content in the file. Only append the new section at the end.
  </action>
  <verify>
    <automated>grep -q '## 5\. Event Log' docs/setup.md && grep -q 'Stats strip' docs/setup.md && grep -q 'Load more' docs/setup.md && grep -q '30 days' docs/setup.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/setup.md contains `## 5. Event Log` section header
    - docs/setup.md contains "Stats strip", "Source filter", "Event table", "Pagination" subsection content
    - docs/setup.md contains status code table with 200, 204, 4xx, 5xx entries
    - docs/setup.md mentions "30 days" retention
    - docs/setup.md mentions "Load more" for pagination
    - All existing content in docs/setup.md is unchanged (sections 1-4 intact)
    - `npm test` passes (no regressions from any file changes)
  </acceptance_criteria>
  <done>docs/setup.md has an Event Log section documenting stats, filtering, pagination, status codes, and retention</done>
</task>

</tasks>

<verification>
1. `npm test` passes — no regressions from package.json version change
2. `npm run lint` passes — no source code changes so should be clean
3. All 7 new/modified files exist with correct content
4. Git tags v1.0.0 and v1.1.0 exist
5. CHANGELOG.md has 3 versioned sections
6. package.json version is 1.2.0
7. release-please manifest matches package.json version
8. Both GitHub Actions workflows have correct triggers and secrets references
9. docs/setup.md has Event Log section
</verification>

<success_criteria>
- CHANGELOG.md documents v1.0.0, v1.1.0, and v1.2.0 with accurate summaries
- package.json version is 1.2.0
- release-please configured in manifest mode bootstrapped at 1.2.0
- GitHub Actions: release.yml runs release-please on push to main
- GitHub Actions: deploy.yml deploys dev on push to main, prod on release
- docs/setup.md includes Event Log tab documentation
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-release-wrapup/09-01-SUMMARY.md`
</output>
